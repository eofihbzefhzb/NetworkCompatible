# Only update version on publishing to Maven Central
# Fork suffix keeps this build from being mistaken for, or resolved instead of,
# the upstream artifact of the same version on Maven Central. The number tracks the
# upstream release this fork is based on; the suffix counts our own releases on top.
version=1.7.4-ip.3
